# Markov FastAPI service — pinned for reproducible deploys.
# Bumps go via PR + ship; see python/markov/README.md.
# Yahoo (yfinance) is intentionally NOT here — price history is fetched
# from FMP via the `requests` library, per the project's kill-Yahoo rule.
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
numpy==2.1.3
pandas==2.2.3
requests==2.32.3
hmmlearn==0.3.3
scikit-learn==1.5.2
